{
  "name": "node-ecma7",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean" : "rm -rf dist && mkdir dist",
    "build" : "./node_modules/.bin/babel -d ./dist ./server -s",
    "start-dev" : "./node_modules/.bin/babel-node server/server.js --inspect",
    "start": "node ./dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "morgan": "^1.9.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  }
}
